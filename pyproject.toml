[project]
name = "litenotify-workspace"
version = "0.1.0"
description = "honker + litenotify + joblite workspace"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-n auto"
